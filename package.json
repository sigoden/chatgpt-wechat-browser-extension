{
  "name": "chatgpt-for-wechat-filehelper",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "lint": "eslint --ext .js,.mjs .",
    "lint:fix": "npx prettier -w . && eslint --ext .js,.mjs . --fix"
  },
  "dependencies": {
    "eventsource-parser": "^0.0.5",
    "expiry-map": "^2.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "archiver": "^5.3.1",
    "esbuild": "^0.16.4",
    "eslint": "^8.29.0",
    "eslint-config-prettier": "^8.5.0",
    "prettier": "^2.8.0",
    "webextension-polyfill": "^0.10.0"
  }
}
